.gem-updates
  h3= @gem['name'].titleize
  h4= @gem['info']
  br= t('.newest_version', version: @gem['version'], date: @newest_version_date)
  br= t('.current_version', version: @gem['version'], date: @newest_version_date)
  br= select_tag :version, options_for_select(@gem_versions)
  br
