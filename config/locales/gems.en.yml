en:
  gems:
    index:
      title: GEM UPDATES
      search: Search
    gem:
      gem_name: "Gem name: %{name}"
      info: "Description: %{info}"
      version_downloads: "Version downloads: %{version_downloads}"
      version: "Version: %{version}"
    show:
      newest_version: "NEWEST VERSION: %{version}, %{date}"
      current_version: "YOUR CURRENT VERSION:"
      gemfile_title: "Gemfile"
      install_title: "Install"
      update_title: "Update"
      gemfile: "gem '%{name}', '~> %{version}'"
      install: "gem install %{name}"
      update: "gem update %{name}"
      clipboard: "Copy to clipboard"
      copy: "Copy"
      dependencies: "DEPENDENCY CHANGES"
    dependencies:
      no_changes: "No dependency changes between these versions"
      latest_dependencies: "The latest dependencies are:"
      dependencies_title: "%{dependency_type} DEPENDENCIES"
    not_found:
      message: "Rubygem '%{name}' could not be found."
